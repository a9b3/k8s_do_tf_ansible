---
- name: Bootstrap | Check if python is installed
  raw: which python
  register: need_python
  ignore_errors: true

- name: Boostrap | Install python 2.x
  raw: DEBIAN_FRONTEND=noninteractive apt-get install -y python-minimal
  when: need_python | failed
